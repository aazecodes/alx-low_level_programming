#include <stdio.h>
/**
 * main - entry point
 *
 * Description: prints from 0 to 9
 *
 * Return: always 0 (success)
 */
int main(void)
{

	for (int digit = 0; digit <= 9; digit++)
	{
	printf("%d", digit);
	}
	printf("\n");
	return (0);
}
